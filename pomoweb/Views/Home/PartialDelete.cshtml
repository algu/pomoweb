@model pomoweb.Domain.ViewModels.PomoFull

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>PomoShort</legend>

    <div class="display-label">
        @Html.DisplayNameFor(model => model.Name)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Name)
    </div>

    <div class="display-label">
        @Html.DisplayNameFor(model => model.Estimate)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Estimate)
    </div>
</fieldset>

@using (Ajax.BeginForm("PartialDelete", new AjaxOptions()
{
    UpdateTargetId = "main",
    InsertionMode = InsertionMode.Replace
}))
{
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Ajax.ActionLink("Back to List", "PartialList", new AjaxOptions()
                {
                    InsertionMode = InsertionMode.Replace,
                    UpdateTargetId = "main"
                })
    </p>
}
